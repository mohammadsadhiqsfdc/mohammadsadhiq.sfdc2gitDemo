public class q1 implements Queueable{

    public void execute(QueueableContext SC){
        
        Account a = new Account(Name='Sample Queueable Batch Account');
        
        insert a;
        
    }
}