public class s1 implements Schedulable{

    public void execute(SchedulableContext SC){
        
        Account a = new Account(Name='Sample Schedulable Batch Account');
        
        insert a;
        
    }
}